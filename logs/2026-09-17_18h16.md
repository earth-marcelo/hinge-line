# 2026-09-17

**Continuação da sessão de 2026-09-15.**

**Pendências da sessão anterior, verificadas:**
- Divergência de três versões do `README.md` (o que foi escrito, o disco, o `HEAD` do git): **não existe mais**. `README.md` no disco e `HEAD` local/remoto (`refs/heads/main` e `refs/remotes/origin/main`, timestamps quase idênticos) estão idênticos — o push já havia acontecido.
- Campo "About" do GitHub: confirmado salvo (aparece no título da página do repositório).
- Encontrado e corrigido um link quebrado em `PROGRESS.md` (apontava para `logs/2026-09-15.md`, arquivo real é `logs/2026-09-15_00h20.md`) — **ainda não commitado** (ver pendência abaixo).

**Feito nesta sessão — esqueleto do pipeline, começando pela extração Sandwell:**
- Confirmado via inspeção do HTML do formulário em `https://topex.ucsd.edu`: a extração por bounding box é um POST para `https://topex.ucsd.edu/cgi-bin/get_data.cgi` com campos `north`, `south`, `west`, `east` (longitude em referência 0–360) e `mag` (`0.1` = gravidade, `1` = topografia/batimetria). Resposta é ASCII puro: `longitude latitude valor` por linha.
- Testado ao vivo (via browser): uma caixa de 12°×16° (~760 mil pontos, 22 MB) respondeu em ~27 s sem erro — sem limite de tamanho aparente, mas request única longa; para áreas maiores vale dividir em blocos.
- Versão atual confirmada: gravimetria **V33.1**, topografia/batimetria V27.1 (a versão 2014 usada em Carvalho et al. 2022 está desatualizada).
- Criados:
  - `src/config.py` — caminhos do projeto e AOI provisória (N -18°, S -30°, O -52°, L -36°, cobrindo Bacia de Santos + FFZ + RJFZ; **ajustar depois** contra a extensão das Figuras 8/9 de Carvalho et al. 2022).
  - `src/acquisition/sandwell.py` — módulo de download (função `fetch_xyz`, `download_aoi`, CLI via `python -m src.acquisition.sandwell`), com retry e log.
  - `requirements.txt` — adicionado `requests`.
  - `.gitignore` — ajustado para permitir versionar a estrutura de subpastas de `data/raw/` e `data/processed/` sem versionar os dados em si.
  - `data/raw/`, `data/raw/sandwell/`, `data/processed/` com `.gitkeep`.
- **Importante:** o ambiente onde estou rodando (sandbox na nuvem) não tem saída de rede liberada para `topex.ucsd.edu` — só testei a chamada real pelo navegador do laptop. O script em si **ainda não foi executado no seu ambiente Python**; só validei sintaxe e a lógica de conversão de longitude. Rodar `python -m src.acquisition.sandwell` (ou importar `download_aoi()`) no laptop é o próximo passo para validar de fato.

**Pendência em aberto (git) — sem shell disponível na ponte remota deste laptop:**
Os arquivos abaixo já estão no disco mas **não commitados**. Rodar no laptop:
```
cd ~/PycharmProjects/hinge-line
git add PROGRESS.md .gitignore requirements.txt src/ data/raw/.gitkeep data/raw/sandwell/.gitkeep data/processed/.gitkeep logs/2026-09-17_18h16.md
git commit -m "Corrige link do PROGRESS.md; adiciona esqueleto do pipeline (extração Sandwell V33.1)"
git push
```

**Outras pendências (sem mudança):**
- Cadastro no BDGON do IBGE (dados gravimétricos terrestres) — portal estava fora do ar em 14/09; alternativa: Dr. Mauro Andrade de Sousa, (21) 2580-7081 / (21) 3504-9129.
- EMAG2v3 (magnetometria): CSV global de 4,5 GB disponível, precisa recorte local — módulo `src/acquisition/emag2.py` ainda não criado.
- GEBCO (batimetria): disponível — módulo `src/acquisition/gebco.py` ainda não criado.
- Depois de validar a extração Sandwell: implementar EMAG2 e GEBCO, depois começar a etapa de separação regional-residual (`src/processing/`, ainda não criada).
